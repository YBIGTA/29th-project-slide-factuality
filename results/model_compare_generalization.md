# generalization(재무) 최종 비교 — M1 · M2 · M2' · M3 · LLM

평가 대상은 `dataset/generalization.jsonl` 의 재무 5개 덱이다 (`finance_01`~`finance_05`, 라벨 유효 538행).

**표가 두 벌인 이유.** LLM 은 전체가 아니라 239행(44%)만 판정했다. 그래서 모델끼리 비교하는 표와, LLM 을 포함해 같은 행으로 비교하는 표를 나눠 놓는다. **두 표의 숫자를 섞어 읽으면 안 된다.**

## 1. 전체 538행 — 모델 4종

| 모델 | macro-F1 | 정확도 | 근거있음 | 무근거 | 모순 | benign |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| M1 (frozen e5 + LinearSVC) | **0.3033** | 0.5651 | 0.710 | 0.109 | 0.100 | 0.295 |
| M2 (KLUE-RoBERTa, 맥락 없음) | **0.2090** | 0.5836 | 0.765 | 0.000 | 0.071 | 0.000 |
| M2' (동일 백본 대조군) | **0.2017** | 0.4238 | 0.692 | 0.061 | 0.054 | 0.000 |
| M3 (KLUE-RoBERTa, 맥락 포함) | **0.1740** | 0.4591 | 0.658 | 0.000 | 0.000 | 0.038 |
| LLM (원문 직접 판정) | — | — | — | — | — | — |

LLM 행이 비어 있는 것은 538행 중 299행을 판정하지 않았기 때문이다. 아래 표 2 를 볼 것.

정답 분포: 근거있음 316 · 무근거 47 · 모순 22 · benign 153

## 2. 공통 239행 — LLM 포함 정면 비교

LLM 이 판정한 행만 골라 다섯을 **같은 행으로** 다시 집계했다. 모델 쪽은 재추론 없이 저장된 행별 예측에서 뽑았다.

| 모델 | macro-F1 | 정확도 | 근거있음 | 무근거 | 모순 | benign |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| **LLM (원문 직접 판정)** | **0.3361** | 0.4435 | 0.586 | 0.351 | 0.385 | 0.023 |
| M1 (frozen e5 + LinearSVC) | **0.2470** | 0.4770 | 0.621 | 0.125 | 0.000 | 0.242 |
| M2' (동일 백본 대조군) | **0.2162** | 0.4770 | 0.780 | 0.056 | 0.029 | 0.000 |
| M3 (KLUE-RoBERTa, 맥락 포함) | **0.1760** | 0.4979 | 0.704 | 0.000 | 0.000 | 0.000 |
| M2 (KLUE-RoBERTa, 맥락 없음) | **0.1739** | 0.5021 | 0.696 | 0.000 | 0.000 | 0.000 |

정답 분포: 근거있음 120 · 무근거 27 · 모순 7 · benign 85

```
LLM · 공통 239행  (행=정답, 열=예측)
               근거있음      무근거       모순   benign
근거있음             90       17       12        1
무근거              16       10        0        1
모순                2        0        5        0
benign           79        3        2        1
```

**LLM 은 'benign' 을 3번밖에 예측하지 않았는데 정답은 85개다.** 슬라이드 제목·목차처럼 검증 대상이 아닌 텍스트까지 '근거있음' 으로 판정했다. `guideline_v2.md` 는 고유명사·소제목을 Benign 으로 빼지 말라고 하고, `results/iaa_v1.md` 는 이 기준이 어노테이터 사이에서도 가장 크게 갈렸다고 기록한다(불일치 유형 1). 이 표의 LLM 점수는 모델 성능만큼이나 **Benign 기준 해석 차이**를 재고 있다.

## 읽을 때 주의할 것

1. **LLM 은 입력 조건이 다르다.** 원문 PDF 와 덱을 통째로 넣고 받은 판정이라 **검색기를 거치지 않는다.** M1~M3 는 검색기가 뽑은 top-5 구간만 보고 판정한다. 즉 이 비교는 '모델 대 LLM' 인 동시에 '검색된 근거만 보기 대 원문 전체 보기' 다. LLM 이 유리한 조건이다.

2. **커버리지 239/538 (44%).** 빠진 행이 무작위가 아니다. 정답 '모순' 22개 중 15개를 LLM 이 보지 않았다. 가장 어려운 클래스가 대거 빠져 있어 LLM 쪽에 유리한 방향으로 치우친다.

3. **매칭 실패 3행** (1.2%). claim_id 로 붙였고 실패분은 claim 본문·공백무시 비교까지 시도했지만 데이터셋에 해당 claim 이 없다.
   - `finance_01__gammaAI_s02_c08` — 본 연구는 지역별 시장구조 비교를 통해 한국 주택시장의 금융연계성과 지역 이질성을 실증적으로 규명한다.
   - `finance_02__chatGPT_s11_c09` — 임대료 상승이 정책 지속 가능성을 훼손할 위험
   - `finance_02__chatGPT_s11_c10` — 초기 매도 물량 소진 후 임대발 가격 압력이 재확대

4. **모델 쪽은 단일 seed 다.** 가중치가 모델당 한 벌뿐이라 3-seed 평균·표준편차를 낼 수 없다. PR #10 의 259행 결과(seed 13/42/777 평균)와 직접 비교하면 안 된다.

5. **M2 의 입력 조립은 가정이다.** 학습 코드가 레포에 없어 M3 형식으로 가정해 돌렸다. 실제 학습 형식과 다를 수 있어 **M2 수치는 보증되지 않는다.**

6. **`finance_03`·`finance_05` 의 원문 정제는 사람 검수 전이다** (종결어미 49.7% / 54.9%). `finance_04` 는 읽기레이어 추출로 75.7%.

7. **후보 구간이 비어 있는 행 17개**(`finance_03` 9 · `finance_05` 8)는 모델이 근거 없이 판정한다. LLM 은 원문을 직접 보므로 이 제약을 받지 않는다.

---

생성: `python src/build_generalization_table.py` · 수치 원본은 `results/model_compare_generalization.json`
